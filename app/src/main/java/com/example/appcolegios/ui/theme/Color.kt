@file:Suppress("unused")

package com.example.appcolegios.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta fresca y accesible
// Marca principal
val Primary = Color(0xFF2D8CF0)       // Azul claro principal
val OnPrimary = Color(0xFFFFFFFF)
// Azul profundo para barras superiores y estados activos
val PrimaryDeep = Color(0xFF1B6FD6)

// Acento celeste para acciones secundarias y microanimaciones
val Secondary = Color(0xFF46B3FF)
val OnSecondary = Color(0xFFFFFFFF)

// Tono extra para detalles/terciario (puede heredarse del acento)
val Tertiary = Color(0xFF46B3FF)

// Fondo limpio blanco azulado
val Background = Color(0xFFF6F9FF)
val OnBackground = Color(0xFF222B45)   // Texto principal (azul grisáceo oscuro)

// Superficies
val Surface = Color(0xFFFFFFFF)
val OnSurface = Color(0xFF222B45)      // Texto principal en superficies
val OnSurfaceVariant = Color(0xFF6B7280) // Texto secundario

// Estados
val SuccessColor = Color(0xFF22C55E)
val WarningColor = Color(0xFFF59E0B)
val ErrorColor = Color(0xFFEF4444)

// Bordes/sombras suaves
val OutlineColor = Color(0xFFE5E7EB)
